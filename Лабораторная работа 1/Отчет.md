# Отчет по лабораторной работе №1, Часть 1: Подготовка рабочего окружения для MLOps

дисциплина: "Технологии программирования" <br>
**Выполнила:** Рябышева Вероника Валерьевна <br>
группа: ПИН-м-о-24-1 <br>
специальность: 09.03.03 Прикладная информатика <br>

**Преподаватель:** Щеголев Алексей Алексеевич <br>
старший преподаватель департамента цифровых, робототехнических систем и электроники института перспективной инженерии

# Цель работы 
Освоить базовые принципы установки и настройки современного рабочего окружения для Data Science и MLOps, основанного на дистрибутиве Anaconda (Conda) и технологии Docker. Получить практические навыки управления виртуальными окружениями Python, работы с менеджером пакетов Conda и выполнения базовых операций с Docker-контейнерами.

# Ход выполнения работы

**1. Загрузка и установка Miniconda:**

В терминале была установлена последняя версия установщика Miniconda для Linux (x86_64) с официального сайта с помощью wget: 

```wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linuxx86_64.sh```

Далее был произведен запуск установщика и активированы изменения путем перезапуска терминала. В итоге, после всех операций появился base в начале строки, что свидетельствует об успешной активации окружения conda.

**2. Создание и активация виртуального окружения:**

Было создано виртуальное окружение с именем mlops-lab и Python версии 3.10 с использованием команды ```conda create -n mlops-lab python=3.10```.
Далее созданное окружение было активировано командой 
```conda activate mlops-lab```.
Начало строки изменилось с base на mlops-lab.

**3. Установка пакетов в созданное окружение:**
Были установлены пакеты numpy, pandas, scikit-learn, matplotlib и jupyterlab командой <br>
```pip install numpy pandas scikit-learn matplotlib jupyterlab```

Для проверки импортировали pandas командой <br>
```python -c "import pandas as pd; print(pd.__version__)```

**Этап 2: Установка Docker и базовые операции**
**1. Установка Docker Engine:**
<br>

Были обновлены индексы пакетов и установлены необходимые зависимости: <br>
```sudo apt-get update``` <br>
```sudo apt-get install ca-certificates curl```

Был добавлен официальный GPG-ключ Docker:
```sudo install -m 0755 -d /etc/apt/keyrings``` <br>
```sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc``` <br>
```sudo chmod a+r /etc/apt/keyrings/docker.asc```

Далее был настроен репозиторий командой
```echo \
"deb [arch=$(dpkg --print-architecture) signedby=/etc/apt/keyrings/docker.asc] \
https://download.docker.com/linux/ubuntu \
$(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

Далее  был установлен Docker Engine: <br>
```sudo apt-get update``` <br>
```sudo apt-get install docker-ce docker-ce-cli containerd.io dockerbuildx-plugin docker-compose-plugin```

Для проверки запустили тестовый образ и получили его в контейнере: <br>
```sudo docker run hello-world```

**2. Настройка прав для не-root пользователя (ОБЯЗАТЕЛЬНО):**

В рамках выполнения данной лабораторной работы был добавлен пользователь в группу docker <br>
```sudo usermod -aG docker $USER```

Для применения изменений был воспроизведен выход и вход в систему.

**3. Базовые команды Docker:**

Были использованы следующие базовые команды:
- Запуск контейнера Ubuntu 24.04: 
```docker run -it ubuntu:24.04 /bin/bash```

- Просмотр работающих контейнеров:
```docker ps``` <br>
```docker ps -a```

- Просмотр образов: 
```docker images``` <br>

- Удаление контейнера:
```docker rm <container_id>```<br>

**4. Запуск JupyterLab в Docker-контейнере:**

Для запуска была использована команда:<br>
```docker run -p 8888:8888 -v $PWD:/home/jovyan/work jupyter/datasciencenotebook:latest```

Далее в логах запуска нашли URL вида http://127.0.0.1:8888/lab?token=... и открыли в браузере.

Следующим этапом создали новый Notebook Python 3 и проверили окружение:<br>
```import numpy as np```<br>
```import pandas as pd```<br>
```print("NumPy version:", np.__version__)```<br>
```print("Pandas version:", pd.__version__)```<br>

Далее контейнер был остановлен командой <br> ```docker stop <container_id>```


# Выводы

В ходе лабораторной работы была освоена установка Miniconda, а также создание и активация виртуальных окружений Python. Были изучены и применены навыки установки базовых пакетов для Data Science, включая numpy, pandas, scikit-learn, matplotlib и jupyterlab. Также был установлен Docker, проверена работа контейнеров и выполнены основные команды для их управления. В рамках работы был запущен JupyterLab внутри Docker-контейнера, после чего была проведена проверка корректности настроенного окружения с помощью Python.



